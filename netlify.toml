[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# The app runs fully in the browser after loading, so any route serves index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
